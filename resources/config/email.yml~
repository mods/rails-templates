development:
  delivery_method: :smtp
  smtp_settings:
    address: smpt.host.com
    port: 25
    domain: smpt.host.com
    authentication: :login
    user_name: smtp@host.com
    password: xxxxxx
    
production:
  delivery_method: :smtp
  smtp_settings:
    address: authsmtp.iaki.it
    port: 25
    domain: authsmtp.iaki.it
    authentication: :login
    user_name: smtp@iaki.it
    password: eoemr9lz

development2:
  delivery_method: :smtp
  smtp_settings:
    address: mail.iaki.it
    port: 25
    domain: mail.iaki.it
    authentication: :login
    user_name: sender
    password: 12mvi7x4
